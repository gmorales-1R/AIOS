# bones — memory

Persistent state and accumulated knowledge across sessions.

## Session history

| Date | Summary |
|------|---------|
| 2026-06-24 | Directory scaffold created. Phaser 3 chosen as framework. Game concept TBD. |
| 2026-06-24 | Asset survey complete. Isometric grid confirmed. First level rendered. |
| 2026-07-05 | **Discovered the `raw_assets/` Kenney packs documented below were never actually committed** — `git log` shows no commit ever added anything under `games/bones/raw_assets/`, and the then-current `game.js` referenced those missing paths directly, so the "first level rendered" state from 2026-06-24 was already broken. The asset inventory/tile-convention/character-sprite sections below describe files that no longer exist anywhere; kept for reference in case the original Kenney packs are ever re-sourced, but do not assume they're present. kenney.nl is also unreachable from this sandboxed environment (network policy blocks it), so re-fetching them isn't currently possible from here either. Concept decided this session: necromancer/witch, unrestricted pet summoning, pets kept basic+scalable (see `this.md`). Replaced the dead Kenney references with procedurally generated placeholders (`code/prepare_assets.py` → `files/assets/tile_dirt.png`, `tile_dirt_alt.png`, `witch.png`, `pet.png`) and vendored Phaser locally (`files/js/phaser.min.js`, no more CDN dependency). Verified headlessly: grid renders, witch renders, tapping tiles summons tinted pets with no cap, counter tracks correctly. |

## Architecture reminders

- Source of truth: `files/` — edit directly, no build step.
- Phaser bundle: local (`files/js/phaser.min.js`), vendored 2026-07-05 — no CDN dependency, matches `swarms`/`zhephone` convention.
- Asset paths: relative to `game.html`, served from `files/assets/` — currently placeholder sprites generated by `code/prepare_assets.py` (Kenney `raw_assets/` paths are dead, see session history).
- Serve with: `python3 -m http.server 8081 --directory games/bones/files`

## Isometric grid system

Confirmed from Kenney's own Tiled sample (`kenney_isometric-miniature-dungeon/Samples/Tiled Sample.zip`):

| Parameter | Value |
|-----------|-------|
| Tile canvas | 256 × 512 px |
| Grid cell | 256 × 128 px (tilewidth × tileheight in Tiled) |
| stepX | 128 px (half canvas width) |
| stepY | 64 px (half cell height) |
| Phaser anchor | `(0.5, 1.0)` — bottom-center of canvas |
| Depth sort | `col + row` ascending (painter's algorithm) |

Placement formula:
```
screen_x = originX + (col - row) * 128
screen_y = originY + (col + row) * 64
```

## Tile conventions (historical — describes missing files, see 2026-07-05 entry above)

- **Floor tiles**: use `_S` face from `kenney_isometric-miniature-dungeon/Isometric/`
  - `dirt_S.png` — plain dirt floor (border/outer)
  - `dirtTiles_S.png` — tiled dirt floor (interior)
  - `stone_S.png` — stone floor (accent)
  - `stoneInset_S.png` — inset stone (center/focal)
- **Bases pack** (`kenney_isometric-miniature-bases`): NOT for floors — use for pedestals, underground entrances, elevated platforms
- **Props**: use `_N` face for objects placed on tiles (barrel, chest, crate, etc.)

## Asset inventory (raw_assets/) — historical, files no longer exist, see 2026-07-05 entry above

| Pack | Files | Format | Role |
|------|-------|--------|------|
| kenney_isometric-miniature-dungeon | 288 tiles + 168 char frames | 256×512 | Core dungeon tiles + 7-costume characters (Idle/Run/Pickup) |
| kenney_isometric-miniature-bases | 160 | 256×512 | Pedestals, platforms, underground entrances |
| kenney_isometric-miniature-farm | 228 | 256×512 | Extra props (fences, planks, barrels) |
| kenney_isometric-miniature-library | 144 | 256×512 | Indoor props (bookshelves, desks) |
| kenney_fantasy-ui-borders | 32×4 variants | 48×48 | 9-slice UI panels |
| kenney_ui-pack-adventure | 128 | 48×64 | Buttons, banners, hexagons (brown/grey/red) |
| kenney_board-game-icons | 200+ | 64×64 | Dice (d4–d20), resources, icons |
| kenney_generic-items | 163 | 104×176 | RPG inventory items |
| kenney_splat-pack | 36 | 256×256 | Hit/splat effects |
| kenney_particle-pack | 80 | 512×512 | Muzzle flashes, particles |
| kenney_light-masks-1.0 | 40+ | PNG | Torch/lantern glow masks |
| kenney_rune-pack | 36 | 50×56 | Rune symbols (magic system) |
| kenney_toon-characters | 698 | 96×128 poses | 2D side-view — menus only (mismatched with isometric) |

## Character sprites (dungeon pack) — historical, files no longer exist, see 2026-07-05 entry above

Path: `kenney_isometric-miniature-dungeon/Characters/Male/`
- Naming: `Male_{variant}_{animation}{frame}.png`
- Variants: 0–6 (7 costume types)
- Animations: Idle (1 frame), Run (10 frames: 0–9), Pickup (10 frames: 0–9)
- Canvas: 256×512 — same as tiles, same anchor `(0.5, 1.0)` — zero alignment work

## Known gaps

- Placeholder art only (procedurally generated) — real art (Kenney or otherwise) would need to be re-sourced and re-committed from outside this sandbox; kenney.nl is unreachable from here.
- Witch is currently static (no movement) — summoning is the only implemented interaction.
- No walls, no obstacles, no pet AI/behavior beyond an idle bob tween.
- No persistence — pet count/positions reset on reload (no save wiring yet, unlike `swarms`).
- Scalability is only demonstrated at ~10 pets in manual testing — not yet stress-tested at the hundreds-scale the "unrestricted" identity implies.

## Planned next: asset agent

Original plan (2026-06-24) to build a Python asset-cataloguing/transformation agent is superseded for now by the simpler `code/prepare_assets.py` generator — revisit the fuller agent idea if/when real source art is available to process instead of procedurally generating placeholders.
